apiVersion: v1
kind: Service
metadata:
  name: {{ include "petclinic-exporter.fullname" . }}
  labels:
    {{- include "petclinic-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "petclinic-exporter.selectorLabels" . | nindent 4 }}
